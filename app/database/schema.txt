CREATE TABLE Users (
        ID INTEGER PRIMARY KEY AUTOINCREMENT,
        user_name TEXT,
        password TEXT,
        session_id INTEGER DEFAULT 0,
        last_activity INTEGER,
        is_saboteur INTEGER DEFAULT 0,
        is_sessions_admin INTEGER DEFAULT 0
);

CREATE TABLE Config (
        max_sessions_users INTEGER DEFAULT 4
);

